// Function to change the text of an HTML element
function changeText() {
   
    document.getElementById("message").innerText = "Text updated using defer!";
  }
  
  // Call the function when the script runs
  changeText();  